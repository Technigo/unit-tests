export const angleCalculator = (turns) => {
  return turns * 360
}

  /* i over analyzed this way too much, trying to define various
  values and creating functions.
  definitively one of those 'it is simpler than you think' */