export const isEmpty = (stringArrayOrObject) => {
  //const type = typeof stringArrayOrObject
  if (stringArrayOrObject.length === 0) return true
  else return false
}

/*   if (stringArrayOrObject.length) true;
  else false */
/*   console.log(stringArrayOrObject.length)
  if (stringArrayOrObject.length === 0 && type === 'string' || 'array' || 'object') {
    return false
  } else return true
} */

/* if (stringArrayOrObject === '' || stringArrayOrObject === [] || stringArrayOrObject === {}) {
  return true
} else return false */